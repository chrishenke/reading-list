html{
    font-size:62.5%;
}
body{
    margin:0;
    padding:0;
    font-size:1.6rem;
    color: brandBlue;
    background:brandGrey; 
}
#root{
    display: grid;
    align-items: center;
    height: 100vh;
    grid-template-columns: minmax(500, 1fr);
}
a{
    color: brandBlue;
}